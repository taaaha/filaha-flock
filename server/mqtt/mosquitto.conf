listener 1883
persistence true
persistence_location /mosquitto/data/
password_file /mosquitto/config/passwd
allow_anonymous false
# Production: terminate TLS in front (nginx/traefik) or add a tls listener on 8883.
